# =============================================================================
# ui 包的初始化模块
# =============================================================================
# 该文件是 ui 包的包初始化文件（package __init__.py）。
# 当其他模块通过 `from ui import ...` 或 `import ui` 导入此包时，
# Python 会首先执行此文件中的代码。
#
# 当前该文件为空，意味着：
#   - ui 包不主动导出任何子模块或子包
#   - 使用者需要通过完整路径导入，例如：
#       from ui.main_window import MainWindow
#       from ui.widgets.styled_widgets import StyledCheckBox
#   - 这是一种常见的惰性导入策略，避免在导入 ui 包时触发
#     大量的 PySide6 / Qt 初始化开销
#
# 如果将来需要提供便捷导入（如 `from ui import MainWindow`），
# 可以在本文件顶部添加相应的 import 语句。
# =============================================================================
